version: '2'

tasks:
  code-checks:
    steps:
      - name: detect-secrets
        when: false
      - name: compliance-checks
        when: false
      - name: static-scan
        when: false
      - name: peer-review
        when: false

  code-build:
    displayName: "Instana Terraform Provider Publish"
    steps:
      - name: build-and-publish
        displayName: "Build and publish Terraform provider to GitHub and Terraform Registry"
        image: goreleaser/goreleaser:latest
        securityContext:
          privileged: true
        script: |
          #!/usr/bin/env bash
          set -eou pipefail

          echo "Fetching variables from SPS Environment Properties..."
          export GITHUB_TOKEN=$(get_env GHP_PAT)
          export GPG_FINGERPRINT=$(get_env GPG_FINGERPRINT)
          export GPG_PRIVATE_KEY=$(get_env GPG_PRIVATE_KEY)
          export GPG_PASSPHRASE=$(get_env GPG_PASSPHRASE)
          export TAG=$(get_env TAG)

          git config --global user.email "instanacd@instana.com"
          git config --global user.name "instanacd"
          
          echo "Environment variables set. Verifying Goreleaser installation..."
          goreleaser --version

          echo "Validating semantic versioning format for release tag: $TAG"
          semver_regex='^(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)\.(0|[1-9][0-9]*)(-((0|[1-9][0-9]*|[0-9A-Za-z-][0-9A-Za-z-]*)(\.(0|[1-9][0-9]*|[0-9A-Za-z-][0-9A-Za-z-]*))*))?(\+[0-9A-Za-z-]+(\.[0-9A-Za-z-]+)*)?$'
          if [[ $TAG =~ $semver_regex ]]; then
              echo "Valid SemVer. Proceeding with release process..."
          else
              echo "Invalid release version format. Please adhere to semantic versioning syntax"
              exit 1
          fi
  
          echo "Ensuring release version $TAG does not already exist..."
          export RELEASE_VERSION="v$TAG"
          if git ls-remote --exit-code --tags origin "refs/tags/$RELEASE_VERSION" >/dev/null 2>&1; then
              echo "Release version $RELEASE_VERSION already exists. Aborting release..."
              exit 1
          fi

          # We use --batch and --no-tty for automation
          echo "Importing GPG Key..."
          echo "$GPG_PRIVATE_KEY" | gpg --batch --import
          
          # 4. Configure GPG to use the passphrase automatically
          # This creates a gpg-agent config to avoid interactive prompts
          mkdir -p ~/.gnupg
          echo "allow-loopback-pinentry" >> ~/.gnupg/gpg-agent.conf
          gpgconf --kill gpg-agent
          
          # This will: build, archive, sign, and push to GitHub
          # The Terraform Registry will then see the update via its own webhook.

          # Actual run command:
          # goreleaser release --clean

          # For testing without publishing, use:
          goreleaser release --snapshot --skip=publish --clean

      - name: unit-test
        when: false
      - name: collect-evidence
        when: false
      - name: setup
        when: false



