version: '2'

tasks:
  code-checks:
    steps:
      - name: detect-secrets
        when: false
      - name: compliance-checks
        when: false
      - name: static-scan
        when: false
      - name: peer-review
        when: false

  code-build:
    displayName: "Instana Terraform Provider Publish"
    steps:
      - name: build-artifact
        displayName: "Build and publish Terraform provider to GitHub and Terraform Registry"
        image: goreleaser/goreleaser:latest
        securityContext:
          privileged: true
        script: |
          #!/usr/bin/env bash
          set -e
    
          echo "Fetching variables from SPS Environment Properties..."
          export GITHUB_TOKEN=$(get_env GHP_PAT)
          export GPG_FINGERPRINT=$(get_env GPG_FINGERPRINT)
          export GPG_PRIVATE_KEY=$(get_env GPG_PRIVATE_KEY)
          export GPG_PASSPHRASE=$(get_env GPG_PASSPHRASE)
          export TAG=$(get_env TAG)
    
          # 1. Identity Setup
          git config --global user.email "instanacd@instana.com"
          git config --global user.name "instanacd"
    
          # 2. GPG Setup (Alpine/goreleaser image)
          if command -v apk &> /dev/null; then
                    apk add --no-cache gnupg gpg-agent
          fi
    
          # 3. GPG Configuration - FORCE Loopback for Passphrase
          # This tells GPG to never ask the user and only look at the caller (GoReleaser/Script)
          export GPG_TTY=$(tty || echo "/dev/tty")
          mkdir -p ~/.gnupg
          chmod 700 ~/.gnupg
          echo "pinentry-mode loopback" > ~/.gnupg/gpg.conf
          echo "allow-loopback-pinentry" > ~/.gnupg/gpg-agent.conf
          echo "disable-scdaemon" >> ~/.gnupg/gpg-agent.conf
    
          # 4. Start the agent daemon
          gpgconf --kill gpg-agent || true
          gpg-agent --daemon --allow-loopback-pinentry || true
    
          # 5. Import Private Key
          echo "Importing GPG Private Key..."
          echo "$GPG_PRIVATE_KEY" | gpg --batch --import
    
          # 6. Preset the Passphrase into the Cache (The "Magic" Fix)
          # Since GoReleaser can't prompt, we pre-load the agent cache with the passphrase
          # for the fingerprint. This satisfies the agent before GoReleaser even starts.
          echo "Pre-loading GPG passphrase into agent cache..."
          KEY_ID=$(gpg --list-secret-keys --with-colons | grep '^sec' | cut -d: -f5 | head -n 1)
          /usr/libexec/gpg-preset-passphrase --preset --passphrase "$GPG_PASSPHRASE" "$KEY_ID" || \
          echo "$GPG_PASSPHRASE" | gpg --batch --pinentry-mode loopback --passphrase-fd 0 --sign --dry-run > /dev/null 2>&1
    
          # 7. Semantic Version Validation
          echo "Validating TAG: $TAG"
          if echo "$TAG" | grep -Eq '^[0-9]+\.[0-9]+\.[0-9]+'; then
                    echo "Tag is valid."
          else
                    echo "Error: TAG '$TAG' is not valid SemVer (X.Y.Z)."; exit 1
          fi
    
          # 8. THE DRY RUN
          # We pass the GPG_PASSPHRASE as an env var specifically for GoReleaser's GPG logic
          echo "Executing GoReleaser Dry Run..."
          goreleaser release --snapshot --skip=publish --clean

      - name: unit-test
        when: false
      - name: collect-evidence
        when: false
      - name: setup
        when: false



